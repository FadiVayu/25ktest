apiVersion: apps/v1
kind: Deployment
metadata:
  name: test
spec:
  replicas: 1
  selector:
    matchLabels:
      app: test
  template:
    metadata:
      labels:
        app: test
    spec:
      containers:
      - name: calc
        image: 25ktest:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 3000
        env:
        - name: NODE_ENV
          value: production
        - name: MODE
          value: test
        - name: MONGO_URI
          value: mongodb://mongo-headless:27017
        - name: KAFKA_BROKERS
          value: '["redpanda:9092"]'
        - name: REDIS_URI
          value: redis://redis:6379
---
apiVersion: v1
kind: Service
metadata:
  name: test
spec:
  ports:
    - port: 3000
  selector:
    app: test
